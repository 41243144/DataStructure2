# 資料結構作業 - HW02

### 作者：41243144

### 日期：2025-05-02

---

## 題目描述
(a) 請比較 implement binary search 與 AVL tree 的資料結構，以及使用這兩種方法在 search, insertion 及deletion 一筆資料時的時間複雜度分別為何?
(b) 設資料輸入順序是: 61, 23, 92, 33, 54, 77, 20, 11, 31, 47, 請推導其 AVL Tree step by step。
(c) 請寫出對於右圖 p 點進行 right rotation 相對的程式片段。

---

## 解析

### (a) 時間複雜度比較

| 方法                | 資料結構      | 搜尋 (Search) | 插入 (Insert) | 刪除 (Delete) |
| ----------------- | --------- | ----------- | ----------- | ----------- |
| **Binary Search** | 已排序陣列(靜態) | O(log n)    | O(n)        | O(n)        |
| **AVL Tree**      | 自平衡二元搜尋樹  | O(log n)    | O(log n)    | O(log n)    |

---
### (b) 右旋程式片段 (C++)

```cpp
// 以節點 p 為根做右旋，回傳新的根節點 q
Node* rightRotate(Node* p) {
 Node* q = p->left;
 Node* r = q->right;

 // 執行旋轉
 q->right = p;
 p->left  = r;

 // 更新高度（若有維護高度資訊）
 updateHeight(p);
 updateHeight(q);

 return q;
}
````

> **註**：以上解析與程式碼由 ChatGPT 生成。
